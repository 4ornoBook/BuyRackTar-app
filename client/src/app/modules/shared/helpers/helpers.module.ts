import { NgModule } from '@angular/core';
import { RoutingHelperService } from './routing-helper/routing-helper.service';

@NgModule({
	providers: [RoutingHelperService],
})
export class HelpersModule {}
